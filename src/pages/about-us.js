import * as React from "react"
import PrimaryLayout from "../layouts/PrimaryLayout"


const Home = ()  => (
  <PrimaryLayout title="About Us">
      <p>
        Lots of text
      </p>
  </PrimaryLayout>
)
export default Home